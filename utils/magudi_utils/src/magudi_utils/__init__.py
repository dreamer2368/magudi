"""magudi_utils — Python utilities for the magudi PDE solver.

Bundles PLOT3D file I/O (`plot3dnasa`, `PLOT3D`), mesh helpers
(`SummationByParts`, `RoundJet`, `SingleBlockCartesian`), the FWH
solver, matplotlib helpers, and the PETSc/TAO L-BFGS optimization
driver (`optim`, `msgrad_test`, `parallel_io`, `inputs`).

See DESIGN.md for the optimizer architecture overview.
"""

__version__ = "0.1.0"
